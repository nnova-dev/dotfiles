#!/usr/bin/env bash

APP_NAME=$1

if [ -z "$APP_NAME" ]; then
    echo "Usage: koda-restart-app <nom_de_l_application>"
    exit 1
fi

pkill -x "$APP_NAME"

sleep 0.5

nohup "$APP_NAME" > /dev/null 2>&1 &
